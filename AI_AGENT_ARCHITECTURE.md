# AI Agent Architecture — مجتمع الصحة والثراء

## الهدف
تحويل «المرشد الذكي» إلى وكيل ذكاء اصطناعي (AI Agent) متعدد القنوات، يبدأ داخل المنصة نصيًا ثم يمتد إلى الصوت، المكالمات، اللقاءات، والتدريب التكيفي.

## المبادئ
1. الوكيل لا يملك وصولًا مباشرًا عشوائيًا إلى قاعدة البيانات.
2. كل قراءة أو إجراء يتم عبر أدوات (Tools) محددة ومصرح بها.
3. هوية العضو تُستخرج من جلسة النظام الحالية، ولا يعتمد الوكيل على رقم عضوية يرسله المستخدم داخل النص.
4. لا ينفذ إجراءات حساسة دون تأكيد صريح من المستخدم.
5. لا يقدم وعودًا مالية أو طبية أو نتائج مضمونة.
6. المحادثة والذاكرة طويلة الأمد ستكون منفصلة عن بيانات الحساب الأساسية.
7. الصوت والاتصال والتسجيل لا يُفعّل قبل إضافة طبقة موافقة وخصوصية واضحة.

## الطبقات
### 1. Agent Core
- فهم الطلب.
- تحديد الهدف.
- اختيار الأداة المناسبة.
- تنفيذ الأداة.
- تحليل النتيجة.
- صياغة الرد.
- تحديد الخطوة التالية.

### 2. Knowledge
- محتوى التدريبات الثمانية.
- أسئلة التدريبات والإجابات النموذجية.
- تعليمات المنصة.
- الأسئلة الشائعة.
- مواد التدريب المعتمدة.

### 3. Tools
المرحلة الأولى:
- get_member_progress
- get_training_status
- get_available_tasks
- get_member_summary
لاحقًا:
- get_training_questions
- create_followup
- schedule_session
- record_followup_result
- leader_team_summary

### 4. Memory
ثلاثة مستويات:
- Session Memory: سياق المحادثة الحالية.
- Member Memory: تفضيلات وتقدم وملاحظات مسموحة.
- Knowledge Memory: محتوى تدريبي ثابت وقابل للإدارة.

### 5. Channels
- Web Chat.
- Voice داخل الموقع عبر Realtime/WebRTC.
- Phone عبر مزود اتصالات.
- Group Meetings.
- لاحقًا WhatsApp/Telegram إذا أضيفت قنوات رسمية.

## مراحل التنفيذ
### المرحلة 1 — Agent Core
- نقطة API آمنة.
- التحقق من session token.
- تحميل سياق العضو من النظام.
- محادثة نصية مرتبطة بسياق حقيقي.
- فصل التعليم عن تنفيذ الإجراءات.

### المرحلة 2 — Tools
إضافة الأدوات واحدة واحدة مع صلاحيات واضحة وسجل تنفيذ.

### المرحلة 3 — Memory
حفظ الذاكرة المفيدة فقط، مع التحكم في ما يتم حفظه.

### المرحلة 4 — Voice
استخدام نموذج Realtime للصوت ثنائي الاتجاه، مع WebRTC للمتصفح.

### المرحلة 5 — Meetings
جلسات فردية وجماعية، أجندة، أسئلة، تقييم، وملخص بعد اللقاء.

### المرحلة 6 — Phone
اتصالات صادرة عبر مزود اتصالات، مع موافقة واضحة وتسجيل نتيجة الاتصال.

## الحالة الحالية
- المشروع لديه بالفعل اتصال OpenAI في api/rpc.js.
- المشروع لديه بيانات تدريبات واختبارات وتقييم آلي.
- المشروع لديه جلسات دخول وSupabase RPC.
- سيتم بناء الوكيل فوق هذه البنية بدل إعادة بناء المنصة.

## سياسة الأدوات
كل أداة يجب أن تحدد:
- الاسم.
- المدخلات.
- البيانات التي تستطيع قراءتها.
- الإجراءات التي تستطيع تنفيذها.
- الدور المسموح له.
- هل تحتاج تأكيدًا من المستخدم.
- سجل التنفيذ.

## نموذج التشغيل
المستخدم → Agent Core → Tool Selection → Tool → Result → Agent → Response

وللصوت:
المستخدم ↔ Realtime Voice Agent ↔ Agent Tools ↔ Supabase

## ملاحظة
لن يتم استبدال «المرشد الذكي» الحالي في الواجهة حتى يتم اختبار Agent Core أولًا، لتجنب التأثير في النسخة العاملة.